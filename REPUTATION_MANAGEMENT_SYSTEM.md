# C2CMarket 信誉管理系统使用与实现说明

> 日期：2026-07-24
>
> 执行者：Codex
>
> 规则版本：`reputation-v2`
>
> 状态：第二版责任归属规则已实现，尚未提交、推送或部署

## 1. 怎么判断买家或卖家靠不靠谱

不要只看星级或一个总分。当前系统按以下顺序提供交易决策依据：

```text
先看有效限制和谨慎状态
  -> 再确认当前是买家还是卖家、拼车还是 API
  -> 再看真实完成数量和证据充足度
  -> 再看责任取消、纠纷和原帖作者验证
  -> 最后看经过样本修正的已验证评价
```

最重要的判断规则：

- `存在有效限制`：相关操作由后端直接阻止，不能被历史好评覆盖。
- `需要谨慎核对`：存在未解决纠纷、近期责任取消异常或原帖作者不一致；这不是自动封禁，但应先核对事实。
- `证据不足`：少于 3 笔可验证完成，不等于信誉差，也不能据此显示高信誉。
- `较可靠`、`高可信`：必须同时满足完成量、完成率、责任取消率、风险和时间条件，不能靠一条五星获得。
- linux.do 信任等级只表示外部社区身份信号，不是 C2CMarket 履约分。

信誉只能反映平台记录到的身份、交易终态、评价和治理事实。C2CMarket 不代收、不托管资金，也不对站外付款、账号使用或服务结果作担保。

## 2. 在哪里查看

| 场景 | 展示内容 |
| --- | --- |
| `/my/reputation` | 本人的买家/卖家、综合/拼车/API 六份信誉快照、原始指标和成长进度 |
| `/me/reputation` | `/my/reputation` 的兼容别名 |
| `/u/{username}` | 公开的买家综合信誉和卖家综合信誉 |
| 拼车列表与详情 | 当前车主的卖家信誉摘要 |
| 拼车申请详情 | 车主看申请人的买家信誉；申请人看车主的卖家信誉 |
| API 市场与服务详情 | 商户的卖家信誉摘要 |
| API 订单详情 | 买家看商户卖家信誉；商户看买家信誉 |
| 我的评价 | 按交易类型和评价方向显示交易对手信誉 |
| 管理端用户页 | 六份快照、原始指标、历史、限制、纠纷裁定、申诉、原帖核验和重算 |

列表使用后端批量查询，不为每一行单独请求信誉数据。

## 3. 角色与业务范围

同一用户最多有六份独立快照：

| 角色 | 综合 | 拼车 | API |
| --- | --- | --- | --- |
| 买家 `buyer` | `overall` | `carpool` | `api` |
| 卖家 `seller` | `overall` | `carpool` | `api` |

不同角色和范围不串数据：

- 拼车车主、API 商户进入卖家信誉；
- 拼车申请人、API 购买方进入买家信誉；
- 综合范围汇总拼车和 API，但不会把买家事实算入卖家；
- purchase intent、中间状态和未确认终态不算已完成交易。

## 4. 信誉状态、等级与证据

### 4.1 风险状态

风险状态优先于等级和徽章展示。

| 状态 | 触发条件 | 业务效果 |
| --- | --- | --- |
| `active` | 没有当前风险条件 | 按正常业务规则处理 |
| `caution` | 有未解决纠纷；或近 90 天至少 3 次责任取消且责任取消率超过 20%；或原帖作者不一致 | 提醒谨慎核对，不自动阻止交易 |
| `restricted` | 存在匹配角色、动作和时间范围的有效限制 | 后端阻止对应操作 |

历史责任未知的取消会单独提示，但不会猜测责任方。

`reputation-v2` 对可由持久化事实证明的超时按角色归责：

- API 订单在付款窗口内未付款，订单以 `payment_timeout` 取消时，计为买家责任取消；系统执行超时不等于系统承担业务责任；
- 拼车申请进入确认期限后，缺少买家确认则计为买家责任，缺少车主确认则计为卖家责任；
- 双方都未确认时双方各承担一次，已经确认的一方不受影响；
- 只有终态存在但状态、事件、取消原因和确认记录仍无法证明责任时，才计入“责任未知”。

### 4.2 表现等级

| 等级 | `reputation-v2` 条件 |
| --- | --- |
| 证据不足 `insufficient` | 可验证完成少于 3 笔 |
| 正常 `normal` | 至少 3 笔，但未同时满足较可靠条件 |
| 较可靠 `reliable` | 状态正常；至少 10 笔完成；完成率至少 95%；责任取消率不高于 5%；无未解决纠纷；近 365 天无已确认重大责任纠纷 |
| 高可信 `high_trust` | 较可靠条件连续保持 90 天；至少 30 笔完成；近 90 天至少 3 笔完成；至少 10 条已验证评价；贝叶斯修正评分至少 4.6 |

等级会随事实下降而降级。没有近期完成时，用户不会仅因等待 90 天自动进入高可信。

### 4.3 证据充足度

| 可验证完成数 | 证据充足度 |
| ---: | --- |
| 0-2 | `low`，证据较少 |
| 3-9 | `medium`，证据一般 |
| 10 及以上 | `high`，证据较充分 |

“状态正常但证据较少”不等于“已经可靠”，页面会同时展示等级和证据充足度。

## 5. 卖家判断清单

查看拼车车主或 API 商户时，重点看：

1. **风险是否优先出现**
   - 有效限制会显示为“存在有效限制”；
   - 未解决纠纷、原帖不一致或近期责任取消异常会显示为“需要谨慎核对”。
2. **卖家范围是否正确**
   - 拼车查看 `seller/carpool`；
   - API 服务查看 `seller/api`；
   - 公共主页可查看 `seller/overall`。
3. **完成和取消事实**
   - 只统计双方或业务状态机确认的完成终态；
   - 看完成率和卖家责任取消率，不把对方责任算到卖家。
4. **评价样本**
   - 评价必须来自真实完成交易；
   - 同时看评价数量和贝叶斯修正评分；
   - 一条五星不会得到较可靠或高可信。
5. **原帖作者**
   - 只有资源状态为 `verified` 才显示“原帖作者已验证”；
   - 仅填写 source URL 不等于验证通过；
   - `pending`、`partial`、`mismatch` 和 `expired` 都不会伪装成已验证。

建议：

- `restricted`：不要继续受限操作。
- `caution`：先查看纠纷、取消责任和原帖核验详情，再决定是否交易。
- `insufficient`：只能说明样本少，首次交易应自行控制风险。
- `reliable/high_trust`：说明历史平台事实较稳定，仍不是平台担保。

## 6. 买家判断清单

车主或 API 商户判断买家时，重点看：

1. 当前页面显示的是 `buyer` 角色，而不是对方的卖家等级；
2. 可验证完成数和证据充足度；
3. 买家责任取消率，尤其是接受后取消或未履行确认义务；
4. 未解决纠纷、已确认责任纠纷和有效限制；
5. 卖家对买家的已验证评价；
6. 是否存在历史责任未知的数据，避免把旧数据错误归责给买家。

拼车申请详情和 API 订单详情会按当前查看者身份自动选择交易对手：

- 车主看申请人的买家信誉；
- 申请人看车主的卖家信誉；
- API 商户看购买方的买家信誉；
- API 买家看商户的卖家信誉。

## 7. 双向评价

拼车和 API 订单共用统一评价模型：

- 仅真实完成交易的参与方可以评价；
- 买家和卖家均可评价对方；
- 完成后有 14 天评价窗口；
- 第一方提交后评价保持 `sealed`，对方看不到评分、标签和说明；
- 双方都提交时，两条评价在同一事务中公开并冻结；
- 截止时间到达后，已提交的单方评价可以公开并冻结；
- 普通用户不能修改已公开评价；
- 创建、公开前修改、公开、迁移和管理员移除均保留 revision 历史；
- 被管理员移除或关联到有效交易排除的评价不进入公开信誉。

评价分数使用贝叶斯修正，默认先验权重为 5：

```text
weightedRating =
  (用户评价数 * 用户平均分 + 5 * 平台平均分)
  / (用户评价数 + 5)
```

平台评价样本不足 20 条时，先验平均分使用中性值 4.0。

成长中心把评价数量和修正评分列为“被动证据”，不会显示“还差 N 条评价”或诱导索要五星。

## 8. 纠纷、申诉与限制

信誉治理区分四类事实：

- 纠纷主体：明确哪位用户是被审查对象，举报人或证人不会因参与案件被处罚；
- 信誉裁定：记录责任、严重度、适用角色、公开原因和内部审计原因；
- 申诉修正：申诉批准会反转关联的信誉裁定和限制影响；
- 有效限制：按角色、动作、开始/结束时间和撤销状态判断是否生效。

未解决纠纷默认只产生 `caution`。只有有效限制会阻止具体操作，包括发布、申请、接单、创建 API 订单、查看受限联系方式和提交评价等。

管理员可以改变事实或治理结果，但不能直接填写 tier、state、confidence 或任意加减信誉分。

## 9. 原帖作者验证

验证是资源级事实，分别绑定具体拼车车源或 API 服务。

| 资源状态 | 页面含义 |
| --- | --- |
| `not_submitted` | 未提交原帖 |
| `pending` | 待核验，不能显示已验证 |
| `verified` | 作者身份与绑定身份核验一致 |
| `mismatch` | 作者不一致，对应卖家范围进入 `caution` |
| `expired` | 旧核验已过期，不能继续显示已验证 |

当 source URL 或绑定的 linux.do 身份发生变化时，旧结论不会继续生效。核验事件采用追加式审计历史，管理员更新使用版本前置条件避免并发覆盖。

## 10. 数据真实性与隐私

真实后端模式遵守以下规则：

- 缺失信誉摘要返回 `null` 或空数组，不注入固定等级、完成数、评价数或纠纷数；
- 后端明确返回的数值 0 才显示为 0；
- 请求失败显示错误和重试，不回退 mock 成功数据；
- 最小公开信誉摘要不能由用户选择性隐藏；
- 允许隐藏的注册时间、活跃时间、详细完成统计等字段继续受隐私设置控制；
- 举报人、内部备注、证据、管理员内部原因和敏感联系方式不公开；
- 公共卡片最多显示两个高价值徽章，风险状态始终先于徽章；
- 文案不使用“平台担保”“绝对可靠”或“官方认证”。

本地演示模式保留显式 mock 数据。所有 mock 分支都位于 `shouldUseRealBackend()` 判断之后，不会进入真实后端路径。

## 11. 主要 API

```http
GET  /api/v1/users/{username}/reputation?scope=overall|carpool|api
GET  /api/v1/me/reputation
GET  /api/v1/reputation/rules
GET  /api/v1/me/reviews
POST /api/v1/me/transactions/{type}/{id}/review
PUT  /api/v1/me/transactions/{type}/{id}/review

GET  /api/v1/admin/users/{id}/reputation
POST /api/v1/admin/users/{id}/reputation/recalculate
POST /api/v1/admin/reputation/recalculate
POST /api/v1/admin/disputes/{id}/reputation-outcome
POST /api/v1/admin/users/{id}/reputation-restrictions
POST /api/v1/admin/reputation-restrictions/{id}/revoke
GET  /api/v1/admin/source-author-verifications/{resourceType}/{resourceId}
PUT  /api/v1/admin/source-author-verifications/{resourceType}/{resourceId}
```

客户端不能提交派生等级、状态、证据充足度、徽章或计算指标。

## 12. 数据库与重算

第一版信誉系统使用 migration 57-61：

| Migration | 内容 |
| --- | --- |
| `000057_reputation_transaction_exclusions` | 异常交易排除、恢复和追加式审计 |
| `000058_reputation_governance` | 纠纷主体、信誉裁定、角色/动作限制和申诉反转 |
| `000059_transaction_reviews` | 拼车/API 双向评价、双盲公开、冻结和 revision |
| `000060_reputation_engine` | 六份信誉快照、历史、dirty 标记和时间失效 |
| `000061_source_author_verification` | 资源级作者核验、审计和信誉联动 |

交易、评价、限制、纠纷、排除、身份或原帖状态变化会使相关快照失效。纯时间变化通过 `next_recalculation_at` 触发重算，包括评价截止、限制结束、滚动时间窗、持续 90 天和原帖验证过期。

从 `reputation-v1` 升级到 `reputation-v2` 不需要改写历史订单或确认记录。旧版本快照在下一次读取时自动失效，并依据现有持久化事实重建。

公共详情、本人页面、列表摘要和管理端均调用同一规则服务。管理员可以重算单个用户，也可以分批重建全部用户。

## 13. 当前验证结果

截至 2026-07-24，本地验证通过：

- Go 全量测试和 `go vet ./...`；
- 前端 43 个测试文件、160 项测试；
- Vue 类型检查；
- `VITE_API_MODE=real` 生产构建；
- OpenAPI 严格 YAML 和 270 个 method/path 路由一致性；
- 61 个 migration 文档与期望版本检查；
- 隔离 PostgreSQL 从 migration 1 升级到 61；
- 默认开发库克隆从 `53|false` 升级到 `61|false`，并完成 `61→53→61` 往返；
- 升级前后 600 个用户、8 个套餐、39 条购买意向和 8 个订单的稳定字段哈希一致；
- 7 项信誉、评价和原帖验证 PostgreSQL 集成测试；
- migration 61 `down 1` 后 `up 1`，最终为 `61|dirty=false`；
- 1440x900 桌面和 390x844 移动端浏览器 smoke；
- 固定真实信誉值、URL 推断验证、调试代码、类型抑制和差异格式扫描。

浏览器 smoke 已覆盖信誉成长页、公开用户页、拼车/API 市场和详情、拼车申请详情以及管理信誉审计。API 订单详情的角色映射由源码回归和 PostgreSQL 测试覆盖，当前 mock 流程未生成可用于截图的订单。

## 14. 尚未执行的发布动作

- 代码仍位于混合未提交工作区，没有自动批量暂存或提交；
- 没有 push、创建 PR 或部署；
- production 尚未应用 migration 54-61；
- migration 51/52 历史冲突已通过恢复已发布文件并顺延未发布 migration 解决；默认本地开发数据库保持原状；
- 前端构建保留第三方 `@vueuse/core` pure-annotation 位置提示，不影响构建成功。

上线前应在目标数据库备份后执行 migration，并按同一版本发布后端和前端。

## 15. 相关文件

- 原始第一版实施任务书：`/Users/lixinjian/Downloads/c2cmarket-reputation-system-codex-implementation-prompt-2026-07-24.md`
- 规则实现：`backend/internal/module/reputation/`
- PostgreSQL 实现：`backend/internal/store/postgres/reputation*.go`
- 前端类型：`frontend/src/types/reputation.ts`
- 前端适配器：`frontend/src/lib/reputationBackend.ts`
- 信誉成长页：`frontend/src/pages/MyReputationPage.vue`
- 公共摘要组件：`frontend/src/components/reputation/`
- OpenAPI：`docs/openapi/c2c-market-api-v1.yaml`
